#ifndef FT_STACK_HPP
# define FT_STACK_HPP

# include <memory>
# include <vector>

namespace ft
{
	template <typename T, class container = vector<T>>
	class	Stack
	{
	private:
		T	*data;
		int	size;
		int	capacity;

	public:
		Stack() : size(0), capacity(10) {
		data = 
	}
}

#endif
